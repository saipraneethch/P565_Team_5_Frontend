// AssignmentSubmissions.js
const AssignmentSubmissions = ({ assignmentId }) => {
    // Fetch the submissions for the given assignmentId
    // Display the submissions
  
    return (
      <h2>Submitted Assignments</h2>
    );
  };
  
  export default AssignmentSubmissions;
  